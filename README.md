### 🚀 LOVABLE PROMPT — Student Course Feedback App (Supabase).

Build a complete Student Course Feedback App using Supabase as the backend.
The app must have two main pages with top navigation:

1️⃣ Submit Feedback

2️⃣ View Feedback

## ✅ Submit Feedback Page Requirements

Create a clean, card-based form containing the following fields:

1. USN (text input)
2. Student Name (text input)
3. Course (dropdown with these exact options):
    CSE, Maths, Physics, AI, DBMS, AIML, SEM, GIT, FSD, OS, DCN, DAA, DSC, Math, Physics, FAFL, MCIoT, UHV, DMS, Chemistry, CNS, Secure Programming
4. Rating (number input or 1–5 star rating)
5. Comments (text area)

## ▶️ On Submit:

Insert data into the Supabase table feedback

Fields to save:
```
usn
student_name
course
rating
comments
created_at (server timestamp)
```

After successful insert:
```
Show a success popup (“Feedback Submitted Successfully!”)
```

## ▶️ Form Validation:

```
Required: USN, student name, course, rating
```

🔧 Supabase Table Schema (Use This)
```
create table feedback (
  id bigint generated by default as identity primary key,
  usn text not null,
  student_name text not null,
  course text not null,
  rating int not null check (rating >= 1 and rating <= 5),
  comments text,
  created_at timestamp with time zone default now()
);
```
Make sure the app uses Supabase API calls for insert + fetch.

## 📄 View Feedback Page Requirements

Display all feedback entries from Supabase in a scrollable card layout.
Each card should show:
```
USN
Student Name
Course
Rating
Comments
Submitted date (format created_at)
```

## ▶️ Page Features:

1. Search Bar (real-time filter across USN, name, course)
2. Refresh Button (re-fetch from Supabase)
3. Results should be fetched sorted by created_at descending.

## 🎨 UI Requirements

1. Clean, modern, minimal UI
2. White background with shaded cards
3. Rounded corners, soft shadows
4. Responsive design for mobile + desktop
5. Simple top navigation tabs: 
    Submit Feedback
6. View Feedback
7. Style buttons in green or blue.
8. Use consistent padding and spacing.


Chat GPT Prompt
```
Generate a complete Lovable prompt for building a Student Course Feedback App using Supabase. The app must include two pages Submit Feedback and View Feedback with navigation. Submit Feedback page should contain fields: USN, student name, course dropdown, rating (1–5), comments. Course dropdown must include: CSE, Maths, Physics, AI, DBMS, AIML, SEM, GIT, FSD, OS, DCN, DAA, DSC, Math, Physics, FAFL, MCIoT, UHV, DMS, Chemistry, CNS, Secure Programming. On submit, save data into a Supabase table named feedback with fields: usn, student_name, course, rating, comments, created_at. Show success popup after submission. View Feedback page should show list of feedback in cards, include a search bar (USN, name, course), refresh button, and fetch sorted results from Supabase. Generate a clean UI description and include a Supabase schema for the feedback table.
```
